# افزونه حمایت مالی زیبال - نسخه 2.1

پلاگین حمایت مالی برای وردپرس با درگاه پرداخت زیبال

## نصب و راه‌اندازی

1. پوشه `zibal-wordpress-donate-master` را در مسیر `/wp-content/plugins/` آپلود کنید
2. افزونه را از طریق منوی 'افزونه‌ها' در وردپرس فعال کنید
3. به منوی 'حمایت مالی زیبال' بروید و تنظیمات را انجام دهید
4. کد درگاه پرداخت خود را وارد کنید
5. از shortcode `[ZibalDonate]` استفاده کنید

## استفاده از Shortcode

### استفاده ساده
```
[ZibalDonate]
```

### استفاده پیشرفته
```
[ZibalDonate 
    title="حمایت از پروژه" 
    description="از پروژه ما حمایت کنید" 
    min_amount="5000" 
    max_amount="1000000"
    required_fields="name,amount,mobile"
    button_text="حمایت کنید"
]
```

## پارامترهای Shortcode

| پارامتر | پیش‌فرض | توضیحات |
|---------|---------|---------|
| `title` | "حمایت مالی" | عنوان فرم |
| `description` | - | توضیحات فرم |
| `min_amount` | 1000 | حداقل مبلغ (ریال) |
| `max_amount` | 50000000 | حداکثر مبلغ (ریال) |
| `required_fields` | "name,amount" | فیلدهای اجباری |
| `show_description` | "true" | نمایش فیلد توضیحات |
| `button_text` | "پرداخت" | متن دکمه |

## فیلدهای قابل استفاده در required_fields

- `name` - نام و نام خانوادگی
- `amount` - مبلغ
- `mobile` - شماره موبایل
- `email` - ایمیل
- `description` - توضیحات

## ویژگی‌های پنل ادمین

### صفحه تنظیمات
- تنظیم کد درگاه پرداخت
- انتخاب واحد پول (تومان/ریال)
- تعیین حداقل و حداکثر مبلغ
- پیام‌های سفارشی
- استایل CSS سفارشی

### صفحه تراکنش‌ها
- مشاهده تمام تراکنش‌ها
- فیلتر بر اساس وضعیت
- جزئیات کامل هر تراکنش
- صفحه‌بندی و جستجو

### صفحه گزارشات
- آمار کلی پرداخت‌ها
- گزارش ماهانه
- نمودارهای تحلیلی
- آمار موفقیت تراکنش‌ها
- **مدیریت خودکار:** همه پرداخت‌های موفق برای سوابق مالی نگهداری می‌شوند
- **پاکسازی هوشمند:** فقط گزارش‌های قدیمی ناموفق/ناتمام پاک می‌شوند و 50 مورد آخر آن‌ها باقی می‌ماند

## امنیت و بهینه‌سازی

### امنیت
- تمام ورودی‌ها sanitize می‌شوند
- استفاده از WordPress nonce
- اعتبارسنجی سمت سرور و کلاینت
- محافظت در برابر حملات رایج

### عملکرد
- کد بهینه‌سازی شده
- استفاده از AJAX برای UX بهتر
- کش کردن تنظیمات
- بارگذاری شرطی فایل‌ها

### Gateway Fallback
- اتصال خودکار به gateway.zibal.ir
- در صورت عدم پاسخ، اتصال به gateway.zibal.io
- لاگ کردن تمام تلاش‌ها برای عیب‌یابی
- دکمه تست اتصال در پنل ادمین

## سازگاری

- ✅ WordPress 5.0+
- ✅ کد افزونه و تست‌های امنیتی: PHP 5.6 تا PHP 8.5
- ℹ️ برای PHP 5.6 تا 7.3 باید از نسخه‌ای از WordPress استفاده شود که همان نسخه PHP را پشتیبانی کند؛ حداقل PHP نسخه‌های جدید WordPress مستقل از این افزونه است
- ✅ تمام مرورگرهای مدرن
- ✅ موبایل و تبلت
- ✅ RTL و فارسی
